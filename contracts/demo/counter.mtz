parameter
  ( Unit          %bump_counter
  | Nat           %reset_counter
  | View Unit Nat %getCount
  );
storage Nat;
code {unpair;
      case
        {drop; push Nat 1; add; nil Operation; pair;}
        {dip {drop}; nil Operation; pair;}
        {view {cdr}}
     };
